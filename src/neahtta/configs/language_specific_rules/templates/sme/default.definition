description: |
  "Default display type for definitions, if there are no other matched
  filters. NB: xpath_args relative to <tg /> node."
entry_args:
  explanation: "./tg/t/re"
--
{{ lemma }} ({{ entry_args.explanation }})
