description: |
  "Default display type for lemmas, if no other templates have matching
  filters. NB: xpath_args relative to <e /> node."
entry_args:
  pos: "./e/lg/l/@pos"
  type: "./e/lg/l/@type"
--
{{ lemma }} ({{ entry_args.pos }})
{# TODO: pos, if coming from morphology, should be a tag or tag-part
   class that has a rendering method to automatically handles tagfilter
   stuff #}
